import { FiliereWithLevel } from '../models/filiere.model'

const gl3: FiliereWithLevel = {
  filiere: { abbreviation: 'gl', name: 'genie logiciel' },
  level: 3,
}

export const AllFiliere = {
  gl3,
}
