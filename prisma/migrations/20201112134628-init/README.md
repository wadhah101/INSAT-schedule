# Migration `20201112134628-init`

This migration has been generated by Wadhah mahrouk at 11/12/2020, 2:46:28 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Professor" (
"id" SERIAL,
    "name" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "WeekDay" (
"id" SERIAL,
    "name" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "Filiere" (
"id" SERIAL,
    "abbreviation" TEXT NOT NULL,
    "name" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "FiliereWithLevel" (
"id" SERIAL,
    "filiereId" INTEGER NOT NULL,
    "level" INTEGER NOT NULL,
    "groupCount" INTEGER NOT NULL DEFAULT 2,

    PRIMARY KEY ("id")
)

CREATE TABLE "Subject" (
"id" SERIAL,
    "name" TEXT NOT NULL,
    "filiereWithLevelId" INTEGER NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "SchoolTime" (
"id" SERIAL,
    "dayId" INTEGER NOT NULL,
    "start" TIMESTAMP(3) NOT NULL,
    "end" TIMESTAMP(3) NOT NULL,
    "group" INTEGER,
    "week" TEXT,
    "schoolSessionId" INTEGER,

    PRIMARY KEY ("id")
)

CREATE TABLE "SessionType" (
"id" SERIAL,
    "name" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "SchoolSession" (
"id" SERIAL,
    "subjectId" INTEGER NOT NULL,
    "professorId" INTEGER NOT NULL,
    "sessionTypeId" INTEGER NOT NULL,

    PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "Professor.name_unique" ON "Professor"("name")

CREATE UNIQUE INDEX "WeekDay.name_unique" ON "WeekDay"("name")

ALTER TABLE "FiliereWithLevel" ADD FOREIGN KEY("filiereId")REFERENCES "Filiere"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Subject" ADD FOREIGN KEY("filiereWithLevelId")REFERENCES "FiliereWithLevel"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "SchoolTime" ADD FOREIGN KEY("dayId")REFERENCES "WeekDay"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "SchoolTime" ADD FOREIGN KEY("schoolSessionId")REFERENCES "SchoolSession"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "SchoolSession" ADD FOREIGN KEY("subjectId")REFERENCES "Subject"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "SchoolSession" ADD FOREIGN KEY("professorId")REFERENCES "Professor"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "SchoolSession" ADD FOREIGN KEY("sessionTypeId")REFERENCES "SessionType"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201112134628-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,76 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Professor {
+  id            Int             @id @default(autoincrement())
+  name          String          @unique
+  SchoolSession SchoolSession[]
+}
+
+model WeekDay {
+  id         Int          @id @default(autoincrement())
+  name       String       @unique
+  schoolTime SchoolTime[]
+}
+
+model Filiere {
+  id           Int                @id @default(autoincrement())
+  abbreviation String
+  name         String
+  levels       FiliereWithLevel[]
+}
+
+model FiliereWithLevel {
+  id         Int       @id @default(autoincrement())
+  filiere    Filiere   @relation(fields: [filiereId], references: [id])
+  filiereId  Int
+  level      Int
+  groupCount Int       @default(2)
+  subject    Subject[]
+}
+
+model Subject {
+  id                 Int              @id @default(autoincrement())
+  name               String
+  filiereWithLevel   FiliereWithLevel @relation(fields: [filiereWithLevelId], references: [id])
+  filiereWithLevelId Int
+  SchoolSession      SchoolSession[]
+}
+
+model SchoolTime {
+  id              Int            @id @default(autoincrement())
+  day             WeekDay        @relation(fields: [dayId], references: [id])
+  dayId           Int
+  start           DateTime
+  end             DateTime
+  group           Int?
+  week            String?
+  SchoolSession   SchoolSession? @relation(fields: [schoolSessionId], references: [id])
+  schoolSessionId Int?
+}
+
+model SessionType {
+  id            Int             @id @default(autoincrement())
+  name          String
+  SchoolSession SchoolSession[]
+}
+
+model SchoolSession {
+  id            Int          @id @default(autoincrement())
+  SchoolTime    SchoolTime[]
+  subject       Subject      @relation(fields: [subjectId], references: [id])
+  Professor     Professor    @relation(fields: [professorId], references: [id])
+  subjectId     Int
+  professorId   Int
+  sessionType   SessionType  @relation(fields: [sessionTypeId], references: [id])
+  sessionTypeId Int
+}
```


