# Migration `20201111000934-ss`

This migration has been generated by Wadhah mahrouk at 11/11/2020, 1:09:34 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "SessionType" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL
)

PRAGMA foreign_keys=OFF;
CREATE TABLE "new_SchoolSession" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "subjectId" INTEGER NOT NULL,
    "professorId" INTEGER NOT NULL,
    "sessionTypeId" INTEGER NOT NULL,

    FOREIGN KEY ("subjectId") REFERENCES "Subject"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("professorId") REFERENCES "Professor"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("sessionTypeId") REFERENCES "SessionType"("id") ON DELETE CASCADE ON UPDATE CASCADE
);
INSERT INTO "new_SchoolSession" ("id", "subjectId", "professorId") SELECT "id", "subjectId", "professorId" FROM "SchoolSession";
DROP TABLE "SchoolSession";
ALTER TABLE "new_SchoolSession" RENAME TO "SchoolSession";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201111000732-school-session..20201111000934-ss
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -66,13 +66,20 @@
   SchoolSession   SchoolSession? @relation(fields: [schoolSessionId], references: [id])
   schoolSessionId Int?
 }
+model SessionType {
+  id            Int             @id @default(autoincrement())
+  name          String
+  SchoolSession SchoolSession[]
+}
+
 model SchoolSession {
-  id          Int          @id @default(autoincrement())
-  SchoolTime  SchoolTime[]
-  subject     Subject      @relation(fields: [subjectId], references: [id])
-  Professor   Professor    @relation(fields: [professorId], references: [id])
-  sessionType String
-  subjectId   Int
-  professorId Int
+  id            Int          @id @default(autoincrement())
+  SchoolTime    SchoolTime[]
+  subject       Subject      @relation(fields: [subjectId], references: [id])
+  Professor     Professor    @relation(fields: [professorId], references: [id])
+  subjectId     Int
+  professorId   Int
+  sessionType   SessionType  @relation(fields: [sessionTypeId], references: [id])
+  sessionTypeId Int
 }
```


